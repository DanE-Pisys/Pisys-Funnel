---
const faqs = [
  {
    q: "Is the audit really free?",
    a: "Yes. The audit is free for a limited period to help UK SMEs understand their cyber risk before an incident occurs."
  },
  {
    q: "How long until I get the board snapshot?",
    a: "We book the scope call within 1 business day and deliver the 1-page snapshot with 30/60/90 actions in about 5 business days."
  },
  {
    q: "Do you need access to our systems?",
    a: "No agents or intrusive access. We use a structured questionnaire, light evidence review, and optional read-only checks you approve."
  },
  {
    q: "Will this turn into a sales pitch?",
    a: "No. You’ll receive the snapshot and priorities with no obligation to buy ongoing services."
  },
  {
    q: "Who is this for?",
    a: "UK businesses with roughly 5–250 staff, especially director/owner-led teams using Microsoft 365."
  },
  {
    q: "How is my data handled?",
    a: "Your details are only used to run the audit and schedule the scope call. We don’t share them externally."
  }
];
---

<section class="section bg-white" id="faq">
  <div class="wrap space-y-5">
    <p class="eyebrow">FAQs</p>
    <h2 class="section-heading">Common questions</h2>
    <div class="divide-y divide-[var(--color-border)] border border-[var(--color-border)] rounded-[var(--radius-card)]">
      {faqs.map((item, idx) => (
        <details class="group open:bg-subtle transition">
          <summary class="flex w-full items-center justify-between gap-3 px-4 md:px-5 py-4 cursor-pointer text-left">
            <span class="text-base md:text-lg font-semibold text-[var(--color-text)]">{item.q}</span>
            <svg class="h-5 w-5 text-[var(--color-muted)] transition group-open:rotate-180" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M6 9l6 6 6-6" />
            </svg>
          </summary>
          <div class="px-4 md:px-5 pb-4 text-[var(--color-text)]">{item.a}</div>
        </details>
      ))}
    </div>
  </div>
</section>
